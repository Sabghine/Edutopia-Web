{% extends 'base.html.twig' %}

{% block title %}Costudyingtype{% endblock %}

{% block body %}
    <h1>Costudyingtype</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ costudyingtype.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ costudyingtype.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('costudyingtype_index') }}">back to list</a>

    <a href="{{ path('costudyingtype_edit', {'id': costudyingtype.id}) }}">edit</a>

    {{ include('costudyingtype/_delete_form.html.twig') }}
{% endblock %}
