<form method="post" action="{{ path('co_studying_delete', {'id': co_studying.id}) }}" onsubmit="return confirm('Are you sure you want to delete this Content?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ co_studying.id) }}">
    <button class="btn btn-primary">Supprimer</button>
    <style>
        .btn {
            display: inline-block;
            padding: 8px 8px;
            cursor: pointer;
            border-radius: 5px;
            background-color: #ADADAD;
            font-size: 16px;
            font-weight: bold;
            color: red;
        }
    </style>
</form>
