{% extends 'base.html.twig' %}

{% block title %}User index{% endblock %}
{% block content %}
    {% block body %}

        <h1>User index</h1>
        <form METHOD="post">


        </form>

        <table class="table">
            <thead>
            <tr>

                <th>Name</th>
                <th>LastName</th>

                <th>Classe</th>

                <th>Nbasbsece</th>
                <th>mail parent</th>
                <th>absent</th>

            </tr>
            </thead>
            <tbody>
            {% for user in users %}
                <tr>

                    <td>{{ user.name }}</td>
                    <td>{{ user.lastName }}</td>

                    <td>{{ user.classe }}</td>

                    <td>{{ user.nbasbsece  }}</td>
                    <td>{{ user.mailParent }}</td>
                    <td>
                        <form method="POST" action="/user/add">
                            <input type="text" value="{{ user.id}}"  name="wissem" hidden>
                            <input type="submit" value="present(e)">
                        </form>
                        <form method="POST" action="/user/vice">

                            <input type="submit" value="vice">
                        </form>



                        <a href="{{ path('user_show', {'id': user.id}) }}">show</a>

                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="18">no records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <a href="{{ path('user_new') }}">Create new</a>
    {% endblock %}
{% endblock %}
