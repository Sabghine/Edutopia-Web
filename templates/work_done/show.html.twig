{% extends 'base.html.twig' %}

{% block title %}WorkDone{% endblock %}
{% block content %}
{% block body %}
    <h1>WorkDone</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>WorkFile</th>
                <td>{{ work_done.workFile }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ work_done.status }}</td>
            </tr>
            <tr>
                <th>Score</th>
                <td>{{ work_done.score }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('work_done_index') }}">Retourner</a>

    <a href="{{ path('work_done_edit', {'id': work_done.id}) }}">Modifier</a>

    {{ include('work_done/_delete_form.html.twig') }}
{% endblock %}
{% endblock %}
