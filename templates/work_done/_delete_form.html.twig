<form method="post" action="{{ path('work_done_delete', {'id': work_done.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ work_done.id) }}">
    <button class="btn">Supprimer</button>
</form>
