{{ form_start(form, {'attr': {'novalidate':'novalidate'}}) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary" style="border-color: #1aa179;background-color: #1aa179">{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}
