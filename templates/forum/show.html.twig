{% extends 'base.html.twig' %}

{% block title %}Details Forum{% endblock %}
{% block content %}
{% block body %}
    <h1>Details forum</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Subject</th>
                <td>{{ forum.subject }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ forum.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('forum_index') }}">Retourner</a>

    <a href="{{ path('forum_edit', {'id': forum.id}) }}">Modifier</a>

    {{ include('forum/_delete_form.html.twig') }}
{% endblock %}
{% endblock %}
