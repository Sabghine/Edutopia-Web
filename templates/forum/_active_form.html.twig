<form method="post" action="{{ path('forum_active', {'id': forum.id}) }}" onsubmit="return confirm('voulez vous activer cette activité ??');">
    <input type="hidden" name="_token" value="{{ csrf_token('active' ~ forum.id) }}">
    <button class="btn btn-primary" style="background-color: #1aa179;border-color: #1aa179">Activer</button>
</form>