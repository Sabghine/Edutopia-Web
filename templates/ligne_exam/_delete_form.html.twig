<form method="post" action="{{ path('ligne_exam_delete', {'idligne': ligne_exam.idligne}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ligne_exam.idligne) }}">
    <button class="btn">Delete</button>
</form>
